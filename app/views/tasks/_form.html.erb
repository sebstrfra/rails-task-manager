<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <%= f.label :completed %>
  <% if @task.new_record?() == false%>
  <%=  f.check_box :completed %>
  <% end %>
  <%= f.submit %>
<% end %>
